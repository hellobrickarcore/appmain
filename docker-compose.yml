version: '3.8'

services:
  yolo-backend:
    build: 
      context: ./server
      dockerfile: Dockerfile
    container_name: hellobrick-yolo
    ports:
      - "3003:3003"
    restart: always
    volumes:
      - ./server/models:/app/server/models
    environment:
      - PORT=3003
