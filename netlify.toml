[build]
  # The directory that contains the deploy-ready HTML/assets
  publish = "website/react-landing/dist"

[[redirects]]
  # Support Single Page Application (SPA) routing: 
  # Redirect all requests to index.html if the file doesn't exist
  from = "/*"
  to = "/index.html"
  status = 200
